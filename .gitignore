
# ODC benchmark venv
benchmarks/.venv/
__pycache__/
*.pyc

# benchmark outputs (regenerable; tests use tempdir fixtures)
composite*.tif
ndvi*.tif
Rplots.pdf
docs
*.aux.xml
tests/testthat/_problems/
.Rprofile

# compiled-code build + coverage artifacts (R CMD INSTALL builds in
# src/; covr's gcov instrumentation drops counters there and a covr/
# working dir in-tree)
src/*.o
src/*.so
src/*.gcda
src/*.gcno
covr/


# scratch files
scratch.R
km*.tif
garry-logo.png
/.quarto/
