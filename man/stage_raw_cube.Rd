% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_adapter.R
\name{stage_raw_cube}
\alias{stage_raw_cube}
\title{Stage a GDAL raster as a raw-BSQ cube (\code{.bin} + sibling \code{.vrt}).}
\usage{
stage_raw_cube(src, dst_vrt, slab_rows = 512L)
}
\arguments{
\item{src}{Source path readable by GDAL.}

\item{dst_vrt}{Destination \code{.vrt} path (the \code{.bin} lands beside it).}

\item{slab_rows}{Rows per read/write slab (memory bound).}
}
\value{
\code{dst_vrt}, invisibly.
}
\description{
Reads every band of \code{src} and writes garry's raw cube format:
band-sequential f32/f64 planes in a \code{.bin}, described by a
\code{VRTRawRasterBand} VRT that carries the georeference. Any GDAL
consumer reads the VRT normally; garry's reader recognises the shape
and reads the bin directly (measured ~9x on a 73-band cube — GDAL's
tile walk costs ~2.2 s per 482 MB window regardless of compression,
the raw read 0.24 s). Use it once on pipeline intermediates that are
read many times (per-year context cubes, prediction stacks).
}
