% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{g_quantize}
\alias{g_quantize}
\title{Quantize physical values to integer digital numbers, on device.}
\usage{
g_quantize(x, scale, offset, nodata, dtype)
}
\arguments{
\item{x}{Traced f32 array (physical values; NaN = nodata).}

\item{scale, offset}{Write quantization: \code{DN = round((x - offset) / scale)}.}

\item{nodata}{Integer sentinel NaN maps to (must fit \code{dtype}).}

\item{dtype}{Integer output dtype (\code{"u8"}, \code{"i8"}, \code{"i16"}, \code{"u16"},
\code{"i32"}).}
}
\value{
Traced array of \code{dtype}.
}
\description{
The sink-side write transform \code{round((x - offset) / scale)} --
clamped to \code{dtype}'s range (as GDAL's conversion would), with NaN
mapped to \code{nodata} BEFORE the integer cast (casting NaN to an
integer is undefined). Runs in f32 on the producer, so every
execution route yields byte-identical digital numbers; the writer
daemon then only writes.
}
\keyword{internal}
