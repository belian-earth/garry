% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_tif.R
\name{write_tif}
\alias{write_tif}
\title{Execute a lazy raster and stream it to a GeoTIFF.}
\usage{
write_tif(
  x,
  path,
  dtype = NULL,
  scale = NULL,
  offset = NULL,
  nodata = NULL,
  cog = FALSE,
  creation_options = NULL,
  overview_resampling = c("average", "nearest", "bilinear", "cubic", "mode", "rms"),
  band_names = NULL,
  distributed = garry_daemons_set()
)
}
\arguments{
\item{x}{A \code{LazyRaster}, a \code{LazyDataset} (bands assembled along the
band axis), a named list of lazy rasters (multi-export: one plan,
one file per sink), or a \code{LazyDatasetGroups} (one file per group via
a \code{{group}} placeholder in \code{path}).}

\item{path}{Destination path. For a named-list input: a directory
(files named \verb{<sink>.tif}) or a named character vector keyed by sink.}

\item{dtype}{Output dtype override (e.g. \code{"i16"}, \code{"u8"}); default
keeps the plan's dtype (usually \code{"f32"}).}

\item{scale, offset}{Quantization affine (see Details). Requires an
integer \code{dtype}; \code{offset} defaults to 0.}

\item{nodata}{Sentinel written to the file and used for NaN demotion,
in stored (DN) units. Required when an integer \code{dtype} output can
contain NaN.}

\item{cog}{Write a Cloud Optimized GeoTIFF (see Details).}

\item{creation_options}{GDAL creation options (\code{"KEY=VALUE"}). With
\code{cog = FALSE} these replace the default tiled-DEFLATE options of the
streamed write; with \code{cog = TRUE} they go to the COG translate pass
(the temporary streamed file keeps the defaults).}

\item{overview_resampling}{COG overview resampling (\code{cog = TRUE}
only). \code{"average"} (default) suits continuous data; use \code{"nearest"}
for categorical outputs like masks.}

\item{band_names}{As in \code{\link[=collect]{collect()}}.}

\item{distributed}{As in \code{\link[=collect]{collect()}}.}
}
\value{
The written path(s), invisibly (expanded per sink/group for
list, directory, and \code{{group}} forms).
}
\description{
The file-writing sibling of \code{\link[=collect]{collect()}}: executes the plan (same
routes, same daemons) and streams the result to \code{path} chunk by chunk,
so the full raster never sits in memory. Returns the path invisibly;
\code{collect()} always returns the in-session array.
}
\details{
\code{dtype} with \code{scale}/\code{offset} quantizes at the sink boundary: values
are stored as \code{round((v - offset) / scale)} (round half to even) and
the affine is written as band scale/offset metadata, so GDAL readers
(QGIS, \code{lazy_source(scale = TRUE)}) recover physical values. An int16
reflectance file is half the raw bytes of float32 and compresses far
better. NaN demotes to \code{nodata}, which is stored in DN units and must
sit outside the quantized data range.

\code{cog = TRUE} streams to a temporary tiled GeoTIFF beside \code{path}, then
finalises with one \code{gdal_translate} pass to the COG driver (which is
copy-only by design: overviews precede full-res data). The extra
sequential pass is the trade every COG producer makes; the temporary
file is removed even on failure, so \code{path} never holds a half-written
COG.
}
