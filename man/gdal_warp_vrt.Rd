% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_adapter.R
\name{gdal_warp_vrt}
\alias{gdal_warp_vrt}
\title{Build a warped VRT of a source onto an exact target grid.}
\usage{
gdal_warp_vrt(src_path, band, target_grid, resampling, src_nodata = numeric(0))
}
\arguments{
\item{src_path}{Source path/VSI URL.}

\item{band}{1-based source band (the VRT has this single band).}

\item{target_grid}{\code{GridSpec} to warp onto.}

\item{resampling}{GDAL resampling method name.}

\item{src_nodata}{Source sentinel (length 0 or 1), from the SourceNode.}
}
\value{
Path to the VRT file (in \code{tempdir()}).
}
\description{
Delegates every pixel of cross-CRS math to the GDAL warper (decision
D5): \code{-te}/\code{-ts} pin the output grid exactly to \code{target_grid}.
Float targets without a source nodata get \verb{-dstnodata nan} so area
outside the source footprint reads as NaN, not 0 (D8).
}
