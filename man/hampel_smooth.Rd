% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_hampel.R
\name{hampel_smooth}
\alias{hampel_smooth}
\title{Hampel filter a stack over its time axis.}
\usage{
hampel_smooth(x, k = 3L, t0 = 3, bands = NULL)
}
\arguments{
\item{x}{A \verb{(t, y, x)} \code{LazyRaster}, or a \code{LazyDataset} (each band
filtered independently).}

\item{k}{Window half-width in time steps (window size \verb{2k + 1}).}

\item{t0}{Threshold in scaled MADs (default 3; 0 = rolling median).}

\item{bands}{\code{LazyDataset} only: bands to filter (default: all).}
}
\value{
The filtered object, same class and grid as \code{x}.
}
\description{
The classic despiking filter: for each time step, a centred window of
\code{k} steps either side supplies a median and a MAD (median absolute
deviation, scaled by 1.4826 to estimate a standard deviation); an
observation further than \code{t0} MADs from the window median is replaced
\emph{by} that median. Missing steps (NaN) are skipped by the window
statistics and stay missing in the output: this filter removes
spikes, it does not fill gaps (compose with \code{\link[=fill_gaps]{fill_gaps()}} or
\code{\link[=kalman_smooth]{kalman_smooth()}} for that).
}
\details{
The window is positional (\code{k} slices, not calendar days), and shrinks
at the series ends. \code{t0 = 0} degenerates to a rolling median. With
\code{t0 > 0}, note the MAD of a window whose valid values are mostly
identical is 0, so any deviating centre is replaced; this is inherent
to the Hampel construction.

Runs through \code{\link[=scan_over]{scan_over()}}, so it plans, fuses, and distributes like
any other kernel, and applies per band over a \code{LazyDataset}.
}
\seealso{
\code{\link[=kalman_smooth]{kalman_smooth()}}, \code{\link[=fill_gaps]{fill_gaps()}}, \code{\link[=scan_over]{scan_over()}}
}
