% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{sample_points}
\alias{sample_points}
\title{Sample a lazy raster or dataset at points.}
\usage{
sample_points(
  x,
  pts,
  method = c("nearest", "bilinear"),
  distributed = garry_daemons_set()
)
}
\arguments{
\item{x}{A \code{LazyRaster} or \code{LazyDataset}. A dataset is assembled along
the band axis first (as \code{\link[=collect]{collect()}} does), so its bands become the
second dimension of the result.}

\item{pts}{A \code{\link[wk:xy]{wk::xy()}} point vector carrying a CRS. Points in another
CRS are reprojected onto the target grid; an unset CRS is an error
rather than an assumption. Convert from sf, terra or a data frame
with \code{\link[wk:as_xy]{wk::as_xy()}}.}

\item{method}{\code{"nearest"} (default) or \code{"bilinear"}; see above.}

\item{distributed}{Execute across the \code{\link[=garry_daemons]{garry_daemons()}} pools? Defaults
to \code{\link[=garry_daemons_set]{garry_daemons_set()}}, as in \code{\link[=collect]{collect()}}.}
}
\value{
A numeric matrix shaped like \code{\link[=collect]{collect()}}'s result without the
spatial axes: one row per point, one column per layer of the sink's
non-spatial axis (bands after a dataset is stacked, or time slices
for a stacked cube), with that axis's labels as column names. Points
outside the raster give an all-\code{NA} row, so the result always aligns
row-for-row with \code{pts}.
}
\description{
Executes the pipeline and returns the values at \code{pts} -- the table
equivalent of \code{\link[=collect]{collect()}}, and the way to feed a model fit without
staging the raster to disk first.
}
\details{
Sampling is a sink, so everything upstream behaves exactly as it would
under \code{\link[=collect]{collect()}}: reads stream through the daemon pools, kernels fuse
onto the reads, and only the gathered table reaches R.

\code{method = "bilinear"} weights the four surrounding cell CENTRES by the
point's fractional position. Contributors that are nodata (\code{NaN}) or
outside the raster are dropped and the remaining weights are
RENORMALISED, so a point near a tile edge or a mask boundary still
yields a value; \code{NaN} comes back only when every contributor is
invalid. \code{method = "nearest"} (the default, matching
\code{\link[=lazy_dataset]{lazy_dataset()}}'s \code{resampling}) takes the containing cell exactly,
which is what packed QA bits and quantised codes need.
}
\seealso{
\code{\link[=collect]{collect()}}, \code{\link[=write_tif]{write_tif()}}
}
