% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduce_multiband.R
\name{medoid}
\alias{medoid}
\title{Medoid reducer over time (multivariate).}
\usage{
medoid(iters = 12L, eps = 1e-07)
}
\arguments{
\item{iters}{Weiszfeld iterations (fixed, unrolled).}

\item{eps}{Distance regulariser (guards the weight at zero distance).}
}
\value{
A reducer \code{fn(x, dims)} for \code{\link[=reduce_over]{reduce_over()}}.
}
\description{
Returns a custom reducer for \code{\link[=reduce_over]{reduce_over()}} computing, per pixel,
the \emph{observed} band-vector nearest the \code{\link[=geomedian]{geomedian()}}: a composite
whose every pixel is a real spectrum from a real date (the
odc/hdstats medoid construction). Use it when downstream analysis
must not see synthetic spectra at all.
}
\details{
Ties (several dates equally near) average; timesteps with any NaN
band are never selected; pixels with no valid timestep return NaN.
Input layout as \code{\link[=geomedian]{geomedian()}}: a \verb{(band, t, y, x)} cube reduced over
\code{"t"}.
}
\seealso{
\code{\link[=geomedian]{geomedian()}}, \code{\link[=reduce_over]{reduce_over()}}
}
