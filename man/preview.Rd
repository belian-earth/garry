% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{preview}
\alias{preview}
\title{Preview a lazy object, a collected array, or a raster file.}
\usage{
preview(
  x,
  bands = NULL,
  max_px = NULL,
  stretch = c(2, 98),
  col = grDevices::hcl.colors(64, "Viridis"),
  legend = NULL,
  main = "",
  axes = TRUE,
  xlab = "",
  ylab = "",
  ...
)
}
\arguments{
\item{x}{A \code{LazyRaster}, \code{LazyDataset}, a matrix/array from \code{collect()}, or a
path to a raster file.}

\item{bands}{Bands to show: 1 for a colour ramp, 3 for RGB. For a
\code{LazyDataset}, band names are also accepted. Defaults to the first 3 bands
(RGB) or band 1.}

\item{max_px}{Longest-axis pixel budget for the render; defaults to the
device size.}

\item{stretch}{Percentile cut \code{c(low, high)} for the colour range, or \code{NULL}
for min/max.}

\item{col}{Colour ramp for single-band plots.}

\item{legend}{Draw a legend? Defaults to \code{TRUE} for single band, \code{FALSE} for
RGB.}

\item{main, axes, xlab, ylab}{Plot title, axes toggle, and axis labels.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly.
}
\description{
A quick, informative plot: single band as a colour ramp with a legend, three
bands as a stretched RGB composite, categorical data (few distinct values)
with discrete colours. Nodata is transparent; a percentile \code{stretch} sets the
colour range; axes come from the grid.
}
\details{
Inputs are reduced before rendering: a file or an array is decimated to the
device (or \code{max_px}); a \code{LazyDataset}/\code{LazyRaster} is re-planned at a coarse
resolution so it fetches only what the preview shows (grid-pinned sources
only; otherwise it collects at full resolution and decimates).
}
