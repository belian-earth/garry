% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy_raster.R
\name{lazy_patch}
\alias{lazy_patch}
\title{Whole-window model op (advanced): apply \code{fn} to the raw padded chunk.}
\usage{
lazy_patch(
  x,
  fn,
  radius,
  out_bands = 0L,
  dtype = "f32",
  kernel_id,
  bytes_px = 512,
  flops_px = 10000
)
}
\arguments{
\item{x}{A \code{LazyRaster} (typically a \verb{(band, y, x)} stack).}

\item{fn}{Model body \code{fn(xpad)}, size-preserving.}

\item{radius}{Halo consumed per side, pixels.}

\item{out_bands}{Output band count; 0 drops the band axis.}

\item{dtype}{Output dtype (default \code{"f32"}).}

\item{kernel_id}{Content identity of the closed-over model.}

\item{bytes_px}{Working-set estimate, bytes per core pixel.}

\item{flops_px}{Compute estimate, flops per core pixel.}
}
\value{
A \code{LazyRaster} on the spatial grid (plus \code{out_bands} bands).
}
\description{
The escape hatch behind model-inference verbs such as \code{ocm_mask()}:
where \code{\link[=focal]{focal()}} materialises a shift list (unusable beyond small
radii), a patch op hands \code{fn} the raw window carrying \code{radius} halo
cells per side and crops the contaminated ring off the result. \code{fn}
must be size-preserving on the last two (spatial) dims, derive every
size from its input's shape, be written in the \verb{g_*} vocabulary, and
reduce the leading band axis to \code{out_bands} channels (0 = a plain
2D result). Not differentiable.
}
\details{
\code{kernel_id} stands in for \code{fn} in kernel signatures: give two calls
the same id ONLY if their fns are interchangeable (same weights,
same code). \code{bytes_px}/\code{flops_px} price the kernel for chunk sizing,
memory admission, and placement, which cannot introspect \code{fn}.
}
