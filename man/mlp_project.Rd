% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/band_mlp.R
\name{mlp_project}
\alias{mlp_project}
\title{An MLP band reducer (predict a trained network across the raster).}
\usage{
mlp_project(
  weights,
  biases,
  center = NULL,
  scale = NULL,
  output_activation = c("identity", "sigmoid")
)
}
\arguments{
\item{weights}{List of layer weight matrices, each \verb{(n_out, n_in)},
applied in order; \code{n_in} of the first layer = number of bands.}

\item{biases}{List of bias vectors (\code{n_out} each), same length as
\code{weights}.}

\item{center, scale}{Optional per-band standardisation applied first:
\code{(x - center) / scale}. Length = number of bands.}

\item{output_activation}{\code{"identity"} or \code{"sigmoid"} on the final
layer (hidden layers are ReLU).}
}
\value{
A function \code{fn(x, dims)} suitable for \code{\link[=reduce_over]{reduce_over()}}
\code{over = "band"}.
}
\description{
Returns an anvl reducer \code{fn(x, dims)} for
\code{reduce_over(cube, fn, over = "band")}: standardises the band vector
(optional), then applies dense layers \verb{act(W_l x + b_l)} with ReLU
between layers and \code{output_activation} on the last, yielding one
value per pixel. Weights come in as plain R matrices -- e.g. torch
\code{nn_linear} layers, whose \verb{$weight} is already \verb{(n_out, n_in)} so
\code{as.matrix(layer$weight)} / \code{as.numeric(layer$bias)} drop straight
in. Dropout layers are inference-time identities: skip them.
}
\details{
NaN in any feature band yields NaN for that pixel (complete-cases
semantics); gate QA upstream by mapping bad pixels' features to NaN.
Target back-transforms (expm1, sinh, ...) compose downstream as an
ordinary \code{lazy_map}.
}
\seealso{
\code{\link[=band_project]{band_project()}} for the linear case, \code{\link[=reduce_over]{reduce_over()}}
}
