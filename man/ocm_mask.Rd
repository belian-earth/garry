% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocm.R
\name{ocm_mask}
\alias{ocm_mask}
\title{Cloud/shadow mask a dataset with native OmniCloudMask.}
\usage{
ocm_mask(
  x,
  red,
  green,
  nir,
  model = ocm_model(),
  where = 1:3,
  open = 0L,
  dilate = 0L
)
}
\arguments{
\item{x}{A \code{LazyDataset} whose slices carry the three bands.}

\item{red, green, nir}{Band names (e.g. \code{"B04"}, \code{"B03"}, \code{"B8A"}).}

\item{model}{An \code{\link[=ocm_model]{ocm_model()}}.}

\item{where}{Classes to mask out (default thick + thin cloud +
shadow).}

\item{open, dilate}{Morphological cleanup, as in \code{\link[=mask]{mask()}}.}
}
\value{
The masked \code{LazyDataset} (OCM band consumed).
}
\description{
The one-step verb: derive the OCM class band from three of the
dataset's bands per time slice, then \code{\link[=mask]{mask()}} every value band with
it (\code{where} selects the masked classes; morphology as in \code{mask()}).
The derived band is consumed by the masking, exactly like a QA
\code{mask_asset}.
}
