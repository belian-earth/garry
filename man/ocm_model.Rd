% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocm.R
\name{ocm_model}
\alias{ocm_model}
\title{Load the native OmniCloudMask model.}
\usage{
ocm_model(weights_dir = NULL, models = "regnety", halo = 128L)
}
\arguments{
\item{weights_dir}{Directory with the OCM v4 safetensors files;
default: \code{GARRY_OCM_WEIGHTS}, else the newest version under the
Python package's cache (\verb{~/.local/share/omnicloudmask}).}

\item{models}{Ensemble members to run (\code{"regnety"}, \code{"edgenext"});
OCM v4 averages both. Default: regnety only until the edgenext
port lands.}

\item{halo}{Chunk overlap margin in pixels (multiple of 32
recommended).}
}
\value{
An \code{ocm_model} list: \code{fn}, \code{kernel_id}, \code{halo}, \code{bytes_px},
\code{flops_px}, \code{models}.
}
\description{
Builds the inference kernel for \code{\link[=ocm_predict]{ocm_predict()}} / \code{\link[=ocm_mask]{ocm_mask()}}: reads
and folds the OCM v4 weights (cached; see \code{\link[=ocm_load_weights]{ocm_load_weights()}}),
closes the forward pass over them, and prices the kernel for the
planner. The result is reusable across any number of scenes and
datasets; all per-slice stages sharing one model collapse to a
single compiled kernel per daemon.
}
\details{
\code{halo} is the overlap margin each chunk recomputes so that chunk
seams carry full spatial context (OmniCloudMask itself blends
overlapping patches; garry crops instead). The per-window
normalisation makes results inherently window-dependent, exactly as
OCM's are patch-dependent, so expect class agreement with the Python
implementation, not bit identity, except in the single-chunk case.
}
