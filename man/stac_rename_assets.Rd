% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{stac_rename_assets}
\alias{stac_rename_assets}
\title{Rename assets to a common band schema.}
\usage{
stac_rename_assets(sources, mapping, drop_unmapped = TRUE)
}
\arguments{
\item{sources}{A \code{stac_sources()} table.}

\item{mapping}{Named character \code{c(old = new)}: original asset -> common name.}

\item{drop_unmapped}{Drop assets not named in \code{mapping} (default \code{TRUE})? When
\code{FALSE}, unmapped assets pass through unchanged.}
}
\value{
The table with a rewritten \code{asset} column.
}
\description{
Harmonising collections that name the same physical band differently (e.g.
HLS Landsat \code{B05} and HLS Sentinel-2 \code{B8A} are both the narrow NIR): supply a
\code{c(old = new)} map and the \code{asset} column is rewritten to the shared names.
Assets absent from the map are dropped by default, so the map doubles as the
band selector. Include identity entries (\code{Fmask = "Fmask"}) to keep a band
under its own name.
}
\details{
After renaming, \code{\link[=stac_merge]{stac_merge()}} concatenates the collections into one table.
A band a collection lacks needs no placeholder: \code{\link[=lazy_dataset]{lazy_dataset()}} gives each
band only the slices that carry it, and \code{\link[=mask]{mask()}} pairs those slices with the
QA band by name (a Landsat-only thermal band masks against the Landsat Fmask
slices), so ragged bands reduce over exactly their own observations.
}
