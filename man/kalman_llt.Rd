% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_kalman.R
\name{kalman_llt}
\alias{kalman_llt}
\title{Kalman local-linear-trend smoother body for \code{\link[=scan_over]{scan_over()}}.}
\usage{
kalman_llt(
  sigma_lvl,
  sigma_slp,
  sigma_obs = 1,
  output = c("mean", "sd"),
  robust_iters = 0L,
  robust_threshold = 3,
  robust_inflation = 100,
  kappa = 1e+07,
  out_dtype = "f32"
)
}
\arguments{
\item{sigma_lvl, sigma_slp, sigma_obs}{Noise standard deviations (level
disturbance, slope disturbance, observation).}

\item{output}{\code{"mean"} (smoothed level) or \code{"sd"} (its standard error).}

\item{robust_iters}{Robust reweighting passes (0 = plain smoother).
Each pass inflates the level noise at years whose smoothed-level
innovation exceeds \code{robust_threshold} MADs by \code{robust_inflation}.}

\item{robust_threshold, robust_inflation}{Robust loop constants.}

\item{kappa}{Diffuse-initialisation variance.}

\item{out_dtype}{Output dtype the body casts to (align with
\code{scan_over(dtype = )}; default \code{"f32"}).}
}
\value{
A scan body \code{fn(xs, margin)} for \code{\link[=scan_over]{scan_over()}}.
}
\description{
Returns a scan body \code{fn(xs, margin)} computing the smoothed level mean
(or its standard error) of a per-pixel local-linear-trend Kalman
filter + RTS smoother over the \code{t} axis, batched over the chunk's
pixels. Use with \code{scan_over(x, kalman_llt(...), direction = "bidir")};
\code{x} is the observation stack, optionally \code{list(x, r)} with \code{r} a
per-year relative observation-variance stack (\code{Var(v_t) = sigma_obs^2 * r_t}; \code{r} must be finite wherever \code{y} is observed).
}
\details{
Hyperparameters are fixed R scalars, fitted off-raster (e.g. hutan's
marginal-likelihood MLE) and closed over as f64 constants. Pixels
with fewer than 3 valid observations return all-NaN (matching
hutan). Initialisation is the large-variance diffuse approximation
\code{P1 = kappa * I}; see the file header for the f64/kappa rationale.

\code{output = "mean"} and \code{output = "sd"} are separate scan bodies (one
export per node); the smoother recomputes per node, which is noise at
T ~ 15 next to IO.
}
\seealso{
\code{\link[=scan_over]{scan_over()}}, \code{\link[=g_scan]{g_scan()}}
}
