% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{fill_gaps}
\alias{fill_gaps}
\title{Fill nodata gaps along the time axis.}
\usage{
fill_gaps(x, method = c("ffill", "bfill", "linear"), over = "t")
}
\arguments{
\item{x}{A \code{LazyRaster} with a \code{t} dim, or a \code{LazyDataset}.}

\item{method}{\code{"ffill"}, \code{"bfill"} or \code{"linear"}.}

\item{over}{Axis to fill along (only \code{"t"} is meaningful today).}
}
\value{
The filled object, same class as \code{x}.
}
\description{
Temporal gap-filling (the verbs xarray spells \code{ffill}/\code{bfill}/
\code{interpolate_na}), implemented as \code{\link[=scan_over]{scan_over()}} bodies:
\itemize{
\item \code{"ffill"} carries the last valid value forward;
\item \code{"bfill"} carries the next valid value backward;
\item \code{"linear"} interpolates between the nearest valid neighbours
(index-weighted, i.e. by slice position, not calendar spacing),
falling back to ffill/bfill at the ends.
}
}
\details{
Works on a \verb{(t, y, x)} \code{LazyRaster} cube or per band of a
\code{LazyDataset}. All-nodata pixels stay NaN.
}
\concept{time series smoothers}
