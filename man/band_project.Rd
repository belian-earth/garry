% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy_raster.R
\name{band_project}
\alias{band_project}
\title{A band reducer for a linear combination of bands.}
\usage{
band_project(weights, center = NULL)
}
\arguments{
\item{weights}{Per-band coefficients (length = number of bands).}

\item{center}{Optional per-band centre subtracted before weighting (e.g. a
PCA's column means); length must match \code{weights}.}
}
\value{
A function \code{fn(x, dims)} suitable for \code{\link[=reduce_over]{reduce_over()}} \code{over = "band"}.
}
\description{
Returns an anvl reducer \code{fn(x, dims)} for \code{reduce_over(cube, fn, over = "band")}: it centres each band (optional) and forms the weighted sum
\verb{sum_b weights[b] * (band_b - center[b])} per pixel -- a linear projection of
the band vector. This is the "reduce over bands" primitive behind spectral
indices, linear/logistic prediction, and PCA. For multiple outputs (e.g. the
first \code{k} principal components) build one reducer per weight column and stack:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{pc <- lapply(1:3, \\(i) reduce_over(cube, band_project(rot[, i], centre),
                                   over = "band"))
collect(lazy_stack(pc, along = "band"))            # (3, y, x)
}\if{html}{\out{</div>}}
}
