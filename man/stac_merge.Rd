% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{stac_merge}
\alias{stac_merge}
\title{Concatenate source tables into one harmonised collection.}
\usage{
stac_merge(...)
}
\arguments{
\item{...}{Two or more \code{stac_sources()} tables (or a single list of them).}
}
\value{
One combined table.
}
\description{
Row-binds \code{stac_sources()} tables (typically after \code{\link[=stac_rename_assets]{stac_rename_assets()}} has
brought them onto a shared band schema) and re-sorts. Every table must carry
the same columns. Same-\code{slice} acquisitions from different collections
co-mosaic (use \code{granularity = "exact"} downstream to keep them as separate
time steps).
}
