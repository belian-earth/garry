% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{stac_filter_assets}
\alias{stac_filter_assets}
\title{Keep only the named assets in a STAC item collection (or sources table).}
\usage{
stac_filter_assets(sources, assets)
}
\arguments{
\item{sources}{A STAC \code{doc_items} or a \code{stac_sources()} data frame.}

\item{assets}{Asset names to keep.}
}
\value{
The filtered \code{doc_items} / data frame.
}
\description{
Drops every other asset from each item -- fewer assets to carry through the
pipeline, and fewer to sign. Apply it BEFORE \code{\link[=stac_sign_mpc]{stac_sign_mpc()}} so only the
assets you keep get a token (MPC returns every band plus thumbnails and
rendered previews). Items left with none of the requested assets are dropped.
Polymorphic: a STAC \code{doc_items} or a \code{stac_sources()} table.
}
