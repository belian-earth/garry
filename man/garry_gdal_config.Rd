% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_adapter.R
\name{garry_gdal_config}
\alias{garry_gdal_config}
\title{Apply garry's default GDAL configuration for remote COG reads.}
\usage{
garry_gdal_config()
}
\value{
Invisibly \code{NULL}.
}
\description{
Sets the GDAL config options the composite / warp-on-read path and
cloud-optimised remote reads want: HTTP multiplexing over HTTP/2, the
odc-stac retry cadence and timeouts, a capped block cache (GDAL
defaults to 5\% of RAM \emph{per process}, which many daemons would
multiply), single-range COG-header ingest, a skipped directory scan
and \code{.tif}-only vsicurl probing for fast remote opens, and the MEM
driver open gate the direct warp needs. \code{garry_daemons()} calls this
on every read daemon automatically; call it yourself for host-side
discovery reads or when you drive \code{mirai::daemons()} directly. Each
option is set via \code{set_config_option}, so a value you set afterwards
wins.
}
\details{
These are session-global GDAL settings. In particular
\code{GDAL_DISABLE_READDIR_ON_OPEN = EMPTY_DIR} speeds remote opens but can
hide sidecars (overviews, world files) for \emph{local} multi-file reads in
the same session; pass \code{gdal_config = FALSE} to \code{garry_daemons()} to
skip it.
}
