% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chunk_grid.R
\name{cross_grid_window}
\alias{cross_grid_window}
\title{Map an output-chunk window on \code{out_grid} to the minimal input window
required on \code{in_grid}.}
\usage{
cross_grid_window(
  out_grid,
  in_grid,
  x_off,
  y_off,
  x_size,
  y_size,
  margin = NULL
)
}
\arguments{
\item{out_grid, in_grid}{\code{GridSpec}s of the consumer and producer.}

\item{x_off, y_off, x_size, y_size}{0-based output window on \code{out_grid}.}

\item{margin}{Safety margin in input cells; defaults to 0 for same-CRS
windows and \code{garry_opt("window_margin")} across CRS.}
}
\value{
A list with the 0-based input window.
}
\description{
Same CRS: exact affine math (north-up grids, so window corners are
extremal). Different CRS: bounds are transformed with
\code{gdalraster::transform_bounds()}, which densifies the boundary so
curved edges (e.g. parallels in a transverse Mercator zone) cannot
shrink the window; a safety margin of \code{garry_opt("window_margin")}
input cells is then added.
}
\details{
Returns a list (x_off, y_off, x_size, y_size), 0-based, clipped to
\code{in_grid}. A window fully outside \code{in_grid} returns zero sizes.
}
