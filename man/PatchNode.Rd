% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node.R
\name{PatchNode}
\alias{PatchNode}
\title{Whole-window model op: fn over the raw padded chunk. Halo-consuming.}
\usage{
PatchNode(
  id = integer(0),
  parents = integer(0),
  grid = GridSpec(),
  role = character(0),
  fn = function() NULL,
  radius = integer(0),
  out_bands = integer(0),
  dtype = character(0),
  kernel_id = character(0),
  bytes_px = integer(0),
  flops_px = integer(0)
)
}
\arguments{
\item{id}{Integer node id (assigned by \code{graph_add()}).}

\item{parents}{Integer ids of parent nodes (length 1).}

\item{grid}{Output \code{GridSpec} of this node.}

\item{role}{Optional semantic role tag (e.g. "mask", set by \code{\link[=mask]{mask()}}).
Pure metadata: never read by the planner or executors; surfaced by
\code{\link[=draw]{draw()}} and \code{\link[=plan_view]{plan_view()}}.}

\item{fn}{The model body \code{fn(xpad)}.}

\item{radius}{Halo consumed per side, pixels.}

\item{out_bands}{Output band count; 0 drops the band axis.}

\item{dtype}{Length-0 (parent's dtype) or length-1 dtype override.}

\item{kernel_id}{Content hash standing in for \code{fn} in kernel
signatures.}

\item{bytes_px}{Resident working-set estimate, bytes per core pixel.}

\item{flops_px}{Compute estimate, flops per core pixel.}
}
\value{
A \code{PatchNode}.
}
\description{
Created by \code{\link[=lazy_patch]{lazy_patch()}}. The stencil shape for kernels whose spatial context is far beyond a
shift-list focal (CNN inference, e.g. OmniCloudMask): \code{fn(xpad)}
receives the parent's value carrying at least \code{radius} halo cells per
side (a \verb{(C, H, W)} cube, traced or plain) and returns a
SIZE-PRESERVING result on the same spatial dims, deriving every size
from the input shape (chunk shapes vary); the evaluator crops the
\code{radius} ring afterwards, exactly as focal shifts consume theirs.
Output rank: 2 when \code{out_bands == 0} (the band axis is consumed),
else 3 with a leading band axis of length \code{out_bands}.
}
\details{
\code{kernel_id} is the content identity of the closed-over model (weights
\itemize{
\item configuration): it stands in for \code{fn} in kernel signatures, so \code{fn}
itself is never serialised or hashed (a model closure can carry tens
of MB). Two nodes with equal \code{kernel_id} are treated as the same
kernel, so \code{kernel_id} must change whenever the model content
changes. \code{bytes_px} / \code{flops_px} are
planner cost hints per CORE pixel; they price chunk sizing, memory
admission, and placement, which cannot introspect an arbitrary model
closure.
}
}
