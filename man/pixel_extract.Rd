% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample.R
\name{pixel_extract}
\alias{pixel_extract}
\title{Extract raster values at points.}
\usage{
pixel_extract(raster, xy, bands = NULL, interp = NULL, ...)
}
\arguments{
\item{raster}{A \code{LazyRaster}, \code{LazyDataset}, raster path, or
\code{GDALRaster} object.}

\item{xy}{Points: a \code{\link[wk:xy]{wk::xy()}} vector (its CRS supplies \code{xy_srs}), or a
two-column matrix/data frame as gdalraster expects.}

\item{bands}{Bands to extract (default all).}

\item{interp}{Interpolation: \code{NULL}/\code{"nearest"} (default), \code{"bilinear"},
\code{"cubic"}, \code{"cubicspline"}.}

\item{...}{Passed to \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}} (\code{krnl_dim},
\code{xy_srs}, \code{max_ram}, \code{as_data_frame}).}
}
\value{
As \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}}: a matrix, or a data frame with
\code{as_data_frame = TRUE}.
}
\description{
garry's extension of \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}}: identical for every
input that function already accepts (a path or a \code{GDALRaster}, passed
through untouched), and additionally accepting a \code{LazyRaster} or
\code{LazyDataset}.
}
\details{
A lazy object has no pixels until it runs, so garry gives GDAL something
to read. When the graph is already a bare source over local files -- what
\code{\link[=materialise]{materialise()}} returns -- those files are read directly. Otherwise the
pipeline is written to a temporary uncompressed cube, sampled, and the
cube removed. Writing first is deliberate: GDAL reads only the
blocks containing points, so extraction from a cube is far cheaper than
computing a whole raster to keep a few thousand cells. If you want the
cube as well, call \code{\link[=materialise]{materialise()}} yourself and pass the result.

\code{xy} may be a \code{\link[wk:xy]{wk::xy()}} point vector, in which case its CRS supplies
\code{xy_srs} and GDAL reprojects as needed; a matrix or data frame behaves
exactly as in gdalraster.
}
\examples{
\dontrun{
pts <- wk::xy(c(512300, 514800), c(4600100, 4601900), crs = "EPSG:32632")
# a lazy pipeline: materialised to a temporary cube, then sampled
pixel_extract(composite, pts, interp = "bilinear")
# keep the cube if you want it for anything else
cube <- materialise(composite)
pixel_extract(cube, pts)
}
}
\seealso{
\code{\link[=materialise]{materialise()}}, \code{\link[=collect]{collect()}}
}
