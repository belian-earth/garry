% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy_raster.R
\name{focal_kernel}
\alias{focal_kernel}
\title{Linear focal op with an explicit kernel (differentiable).}
\usage{
focal_kernel(x, weights, boundary = "nodata")
}
\arguments{
\item{x}{A \code{LazyRaster}.}

\item{weights}{Square odd-sided numeric matrix, rows = dy, cols = dx.}

\item{boundary}{Boundary policy; only "nodata" in v1.}
}
\value{
A \code{LazyRaster}.
}
\description{
The kernel is a (2r+1) x (2r+1) matrix of weights; the op is the
weighted sum over the window. Unlike \code{focal()} with an arbitrary
\code{fn}, a kernel focal is differentiable with respect to its weights:
pass the returned LazyRaster as \code{wrt} to \code{lazy_value_and_grad()}.
}
