% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_adapter.R
\name{.rio_direct_spec}
\alias{.rio_direct_spec}
\title{Can this warp be served by a plain decimating RasterIO read?}
\usage{
.rio_direct_spec(
  src_path,
  target_grid,
  resampling,
  open_options = character(0)
)
}
\arguments{
\item{src_path}{Source path or VSI URL.}

\item{target_grid}{The \code{WarpNode} target \code{\link[=GridSpec]{GridSpec()}}.}

\item{resampling}{Requested resampling name.}

\item{open_options}{Source open options; any at all keep the warper
(GTI and other option-driven sources are left alone in v1).}
}
\value{
\code{NULL}, or a list with \code{fx}, \code{fy}, \code{x_off}, \code{y_off}, \code{resamp}.
}
\description{
A \code{WarpNode} whose target grid shares the source CRS, is axis-aligned,
starts on a source pixel boundary and has a whole-number pixel size
needs no warping at all: GDAL's RasterIO reads the window and
resamples it in one pass, which is measurably cheaper than building a
warped VRT and reading through the warper (2.6x on a 4-band Landsat
crop+3x-average, 0.98s -> 0.375s). Results are identical, including
overview selection and nodata handling at fill boundaries, because
both paths hand the same request to the same GDAL resampler.
}
\details{
Returns \code{NULL} (keep the warper) unless every condition holds, so the
warper stays the default and this is a pure opt-in fast path.
}
\keyword{internal}
