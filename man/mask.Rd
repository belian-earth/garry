% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{mask}
\alias{mask}
\title{Mask a dataset from a QA band.}
\usage{
mask(x, from = NULL, where, open = 0L, dilate = 0L, drop = TRUE)
}
\arguments{
\item{x}{A \code{LazyDataset}.}

\item{from}{QA band to derive the mask from; defaults to the dataset's
\code{mask_asset}.}

\item{where}{The removal predicate (mask where TRUE). One of:
\itemize{
\item a numeric vector -> value membership (bad if the pixel value is in the
set), for categorical QA such as Sentinel-2 SCL,
e.g. \code{c(0, 1, 2, 3, 8, 9, 10, 11)};
\item \code{qa_bits(bits)} -> a bitmask test (bad if any listed bit is set), for
packed flags such as HLS Fmask / Landsat QA_PIXEL, e.g. \code{qa_bits(0:3)};
\item a function \verb{\\(f) ...} -> a raw anvl predicate returning a 0/1 (or logical)
mask.
}}

\item{open}{Opening radius (despeckle): erosion then dilation at this radius,
removing isolated flagged pixels up to the radius. \code{0} skips it.}

\item{dilate}{Dilation radius (buffer): grows the surviving bad regions
outward, a safety margin around clouds. \code{0} skips it. Applied after \code{open}.}

\item{drop}{Drop the QA band from the returned dataset? (default \code{TRUE}.)}
}
\value{
A \code{LazyDataset} with masked value bands.
}
\description{
Derive a bad-pixel mask from a named QA band, optionally clean it with binary
morphology, set bad pixels to NaN (nodata) on every value band, and drop the
QA band. The mask is a shared subgraph computed once per slice and reused
across all value bands.
}
