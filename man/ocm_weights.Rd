% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocm_weights.R
\name{ocm_fetch_weights}
\alias{ocm_fetch_weights}
\alias{ocm_load_weights}
\title{OmniCloudMask model weights}
\usage{
ocm_fetch_weights(dir = NULL, quiet = FALSE)

ocm_load_weights(dir, models = c("regnety", "edgenext"))
}
\arguments{
\item{dir}{For \code{ocm_fetch_weights()}: destination directory
(default: \code{tools::R_user_dir("garry", "data")/ocm-v4}). For
\code{ocm_load_weights()}: directory containing the OCM v4
\code{.safetensors} files.}

\item{quiet}{Suppress progress output.}

\item{models}{Which ensemble members to load
(\code{"regnety"}, \code{"edgenext"}).}
}
\value{
\code{ocm_fetch_weights()} returns the weights directory,
invisibly. \code{ocm_load_weights()} returns a list with \code{weights} (one
entry per model), \code{kernel_id}, and \code{paths}.
}
\description{
\code{ocm_fetch_weights()} downloads garry's mirror of the official
OmniCloudMask v4 weights (two safetensors files, about 58 MB total,
unmodified from upstream; MIT licensed by DPIRD-DMA, see the
release's \code{NOTICE.md} for attribution and citation) into a per-user
data directory, verifying each file's content hash. It is
idempotent: files already present and intact are not re-downloaded.
\code{\link[=ocm_model]{ocm_model()}} finds this directory automatically, so most users need
nothing beyond a one-off \code{ocm_fetch_weights()}.
}
\details{
\code{ocm_load_weights()} is the lower-level loader \code{ocm_model()} uses:
it reads the safetensors state dicts, folds batch norms into their
convolutions, and returns the nested weight lists the native forward
pass consumes. Results are cached as an \code{.rds} under
\code{tools::R_user_dir("garry", "cache")}, keyed by the content hash of
the weight files. Call it directly only to point at a non-standard
weights directory, for example the Python package's download cache
(\verb{~/.local/share/omnicloudmask/<version>/}).
}
\seealso{
\code{\link[=ocm_model]{ocm_model()}}, \code{\link[=ocm_mask]{ocm_mask()}} and \code{\link[=ocm_predict]{ocm_predict()}} for running
the model; \code{\link[=safetensors_read]{safetensors_read()}} for the underlying file format.
}
