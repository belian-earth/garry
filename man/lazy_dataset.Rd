% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{lazy_dataset}
\alias{lazy_dataset}
\title{Build a lazy dataset from a STAC source table.}
\usage{
lazy_dataset(
  sources,
  grid,
  assets,
  mask_asset = NULL,
  granularity = "day",
  sort_field = "datetime",
  nodata = NULL,
  lon = NULL,
  resampling = "near",
  scale = FALSE,
  offset = NULL
)
}
\arguments{
\item{sources}{A STAC \code{doc_items} (from \code{\link[=stac_query]{stac_query()}}, optionally filtered)
or a \code{stac_sources()} table.}

\item{grid}{Target \code{\link[=GridSpec]{GridSpec()}} for every band.}

\item{assets}{Character vector of value assets to load.}

\item{mask_asset}{Optional QA/mask asset (e.g. \code{"Fmask"}, \code{"SCL"}); loaded
alongside the value assets and used as the default \code{from} in \code{\link[=mask]{mask()}}.}

\item{granularity}{Time-slice granularity (see \code{\link[=stac_time_slices]{stac_time_slices()}}).}

\item{sort_field}{Index field ordering overlaps within a slice.}

\item{nodata}{Nodata handling: \code{NULL} (per-asset file metadata), a scalar
(applied to every asset), or a named numeric keyed by asset (unnamed assets
fall back to file metadata). Reflectance and QA bands usually need
different sentinels, so the named form is typical, e.g.
\code{c(B04 = -9999, B03 = -9999, Fmask = 255)}.}

\item{lon}{Longitude for \code{granularity = "solar_day"} (see
\code{\link[=stac_time_slices]{stac_time_slices()}}).}

\item{resampling}{GDAL resampling for the warp-on-read onto \code{grid}: a scalar
(every value band) or a named character keyed by asset (unnamed assets fall
back to \code{"near"}). \code{mask_asset} is always read \code{"near"} regardless, since
interpolating packed QA bits corrupts them. \code{"near"} (the default)
preserves exact source values; use \code{"bilinear"}, \code{"average"}, \code{"cubic"},
... to interpolate. Resample after the fact instead with \code{\link[=align]{align()}}.}

\item{scale}{Apply each value band's scale/offset at read. \code{FALSE}
(default) reads raw digital numbers. \code{TRUE} discovers the affine from
the assets' file metadata (the GDAL band scale/offset QGIS applies;
one asset per band is probed and the collection is assumed
homogeneous) and every read returns \code{v * scale + offset}, applied
after the nodata sentinel becomes NaN. A scalar or named numeric
(keyed by asset) supplies scales explicitly. Discovery only consults
the files themselves: STAC metadata is never read, so collections
whose files carry no scaling metadata (e.g. Planetary Computer
Sentinel-2 L2A) read raw and scale explicitly instead.
\code{mask_asset} is never scaled.}

\item{offset}{Explicit additive offset(s) (scalar or named by asset)
used when \code{scale} is numeric; defaults to 0. Ignored when \code{scale} is
logical.}
}
\value{
A \code{LazyDataset}.
}
\description{
One band per asset, each a time-sliced GTI mosaic pinned to \code{grid} (mixed
source CRS is fine; the GTI driver reprojects per tile). All bands share one
intermediate representation (IR) graph, so a mask defined once (see
\code{\link[=mask]{mask()}}) is computed once and dedup'd across bands, and \code{\link[=collect]{collect()}} plans
the whole dataset in one pass.
}
\seealso{
\code{\link[=lazy_cog]{lazy_cog()}}, \code{\link[=group_by_time]{group_by_time()}}, \code{\link[=collect]{collect()}}
}
