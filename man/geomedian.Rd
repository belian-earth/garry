% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reduce_multiband.R
\name{geomedian}
\alias{geomedian}
\title{Geometric median reducer over time (multivariate).}
\usage{
geomedian(iters = 12L, eps = 1e-07)
}
\arguments{
\item{iters}{Weiszfeld iterations (fixed, unrolled).}

\item{eps}{Distance regulariser (guards the weight at zero distance).}
}
\value{
A reducer \code{fn(x, dims)} for \code{\link[=reduce_over]{reduce_over()}}.
}
\description{
Returns a custom reducer for \code{\link[=reduce_over]{reduce_over()}} computing, per pixel,
the band-vector minimising the sum of Euclidean distances to the
observed band-vectors: the geometric (L1, spatial) median. Unlike a
per-band median, whose result can be a spectrum no real observation
ever had, the geometric median is a genuine multivariate central
tendency: every distance couples all bands.
}
\details{
Solved by Weiszfeld iteration (a fixed-point weighted mean,
weights inverse distance to the current estimate), unrolled to a
fixed \code{iters} steps so it compiles to a static kernel. Timesteps
with any NaN band are ignored; pixels with no valid timestep return
NaN.

The input must be a \verb{(band, t, y, x)} cube: stack each band's
\verb{(t, y, x)} stack along \code{"band"}, then reduce over \code{"t"}; the band
axis survives.
}
\seealso{
\code{\link[=medoid]{medoid()}}, \code{\link[=reduce_over]{reduce_over()}}; \code{\link[=band_project]{band_project()}} and
\code{\link[=mlp_project]{mlp_project()}} for projections over the surviving band axis.
}
