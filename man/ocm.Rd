% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocm.R
\name{ocm_model}
\alias{ocm_model}
\alias{ocm_predict}
\alias{ocm_mask}
\title{Cloud and shadow masking with OmniCloudMask}
\usage{
ocm_model(weights_dir = NULL, models = c("regnety", "edgenext"), halo = 128L)

ocm_predict(red, green, nir, model = ocm_model())

ocm_mask(
  x,
  red,
  green,
  nir,
  model = ocm_model(),
  where = 1:3,
  open = 0L,
  dilate = 0L
)
}
\arguments{
\item{weights_dir}{Directory with the OCM v4 safetensors files.
Defaults to the \code{GARRY_OCM_WEIGHTS} environment variable if set,
then the \code{\link[=ocm_fetch_weights]{ocm_fetch_weights()}} download directory, then the newest
version under the Python package's cache
(\verb{~/.local/share/omnicloudmask}).}

\item{models}{Ensemble members to run; the default matches
OmniCloudMask v4 exactly (both U-Nets, logits averaged). A single
member is roughly twice as fast at slightly lower accuracy.}

\item{halo}{Chunk overlap margin in pixels (multiple of 32
recommended).}

\item{red, green, nir}{For \code{ocm_predict()}: band \code{LazyRaster}s on the
same grid and graph. For \code{ocm_mask()}: names of the dataset bands
to predict from (e.g. \code{"B04"}, \code{"B03"}, \code{"B8A"}).}

\item{model}{An \code{ocm_model} object, from \code{ocm_model()}.}

\item{x}{A \code{LazyDataset} whose slices carry the three bands.}

\item{where}{Classes to mask out (default thick cloud, thin cloud,
and shadow).}

\item{open, dilate}{Morphological cleanup, as in \code{\link[=mask]{mask()}}.}
}
\value{
\code{ocm_model()} returns an \code{ocm_model} object; \code{ocm_predict()}
a class \code{LazyRaster} on the shared spatial grid; \code{ocm_mask()} the
masked \code{LazyDataset} (class band consumed).
}
\description{
Native (no-Python) implementation of the OmniCloudMask v4 cloud and
shadow segmentation model, which predicts per-pixel classes from
red, green, and NIR reflectance at 10-50 m resolution. Three
functions cover the workflow:
}
\details{
\itemize{
\item \code{ocm_model()} loads the pre-trained weights (see
\code{\link[=ocm_fetch_weights]{ocm_fetch_weights()}}) and builds the reusable inference kernel.
One model object serves any number of scenes and datasets; all
stages sharing a model compile to a single kernel per worker.
\item \code{ocm_predict()} runs the model over three band \code{LazyRaster}s and
returns the class band as a new \code{LazyRaster}. Like every garry
verb it is lazy: nothing reads or computes until \code{\link[=collect]{collect()}}.
\item \code{ocm_mask()} is the one-step verb for a \code{LazyDataset}: it derives
the class band from three of the dataset's bands for every time
slice, then masks every value band with it via \code{\link[=mask]{mask()}}. The
derived class band is consumed by the masking, exactly like a QA
\code{mask_asset}.
}

Predicted classes are 0 (clear), 1 (thick cloud), 2 (thin cloud),
and 3 (cloud shadow), with \code{NaN} wherever the input had nodata.

\code{halo} is the overlap margin each chunk recomputes so that chunk
seams carry full spatial context (OmniCloudMask itself blends
overlapping patches; garry crops instead). The per-window
normalisation makes results inherently window-dependent, exactly as
OmniCloudMask's are patch-dependent, so expect class agreement with
the Python implementation, not bit identity, except in the
single-chunk case.

Weights are the OmniCloudMask authors'
(\url{https://github.com/DPIRD-DMA/OmniCloudMask}) and are not
distributed with garry; download them once with
\code{\link[=ocm_fetch_weights]{ocm_fetch_weights()}}.
}
\examples{
\dontrun{
ocm_fetch_weights()  # once per machine
ds <- ds |> ocm_mask(red = "B04", green = "B03", nir = "B8A")
composite <- ds |> reduce_over("time", "median") |> collect()
}
}
\seealso{
\code{\link[=ocm_fetch_weights]{ocm_fetch_weights()}} to download the weights; \code{\link[=mask]{mask()}} and
\code{\link[=qa_bits]{qa_bits()}} for masking from an existing QA band;
\code{vignette("omnicloudmask", package = "garry")} for a worked
example.
}
