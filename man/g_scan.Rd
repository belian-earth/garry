% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{g_scan}
\alias{g_scan}
\title{Scan: carry state along dim 1, emitting per-step outputs.}
\usage{
g_scan(init, body, xs = NULL, length = NULL, reverse = FALSE)
}
\arguments{
\item{init}{Initial carry: array or (nested) named list of arrays.}

\item{body}{Step function \verb{function(carry, x) -> list(carry, out)};
\code{out} may be an array, a (nested) list, or \code{NULL}.}

\item{xs}{Per-step inputs sliced along dim 1 (array, nested list, or
NULL).}

\item{length}{Static trip count; required when \code{xs} is NULL.}

\item{reverse}{Run steps in reverse order?}
}
\value{
\verb{list(carry = final carry, out = stacked outputs)}.
}
\description{
The loop op behind \code{\link[=scan_over]{scan_over()}} bodies (Kalman smoothers, EWMA, IIR
filters, cumulative custom ops). At step \code{t}, \code{body(carry, x)} gets
the current carry and the step-\code{t} slice of \code{xs} (taken along dim 1
with that unit axis dropped) and returns \code{list(carry = , out = )};
the \code{out}s are stacked into \verb{(length, ...)} buffers. \code{reverse = TRUE}
runs \code{t = length..1}, still reading and writing at position \code{t} (what
an RTS backward pass needs). Traced values route to \code{anvl::nv_scan};
plain R arrays take the pure-R oracle loop with identical semantics.
}
\details{
The scanned axis must be dim 1 of every \code{xs} leaf (garry's canonical
layout puts the scanned non-spatial axis first); bodies scanning a
non-leading margin must permute first.
}
