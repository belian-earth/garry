% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_view.R
\name{plan_view}
\alias{plan_view}
\title{Interactive Plan viewer.}
\usage{
plan_view(
  x,
  level_separation = NULL,
  node_spacing = 90,
  height = "600px",
  width = "100\%"
)
}
\arguments{
\item{x}{A \code{Plan} from \code{\link[=plan_lazy]{plan_lazy()}}, a \code{LazyRaster}, a
\code{LazyDataset} (its bands are assembled along the band axis first,
as in \code{\link[=collect]{collect()}}), or a named list of \code{LazyRaster}s.}

\item{level_separation}{Horizontal distance between topological
levels, in pixels. \code{NULL} (default) adapts to the graph's shape:
wide plans (many sources, few levels) get a landscape aspect so
the left-to-right flow stays legible, floored by label clearance
and capped for deep plans. Pass a number to override.}

\item{node_spacing}{Distance between nodes within a level, in
pixels — vertical, since levels run left to right. The tall
stretch of a wide plan (e.g. one source per time slice) is this
times the widest level's node count; lower it to compress.}

\item{height, width}{Widget size, as CSS units.}
}
\value{
A \code{visNetwork} htmlwidget.
}
\description{
Renders a \code{Plan} as an interactive DAG (an htmlwidget, via the
suggested \pkg{visNetwork} package): one node per stage, laid out
left to right in execution order on explicit topological levels.
Stages are labelled by what they compute, not just their scheduler
kind: a compute stage is classified by the IR nodes fused into it
(\code{focal}, \code{scan}, \code{patch}, \code{stack}, \code{map}, most informative first,
the same vocabulary as \code{\link[=draw]{draw()}}), and reduce stages carry their
reducer (\verb{reduce·median}). When a \code{LazyDataset} is passed, derived
bands (\code{ds[["ndvi"]] <- ...}) are recovered from the dataset's step
record and the stage computing one is labelled with the band name
(\verb{derive·ndvi}); the derivation is bounded structurally at the first
non-elementwise node, so it survives subsetting (\code{ds["ndvi"]}). Hovering a stage shows its full op
composition, members, halo, device, and output grid; clicking
highlights its neighbours; the sink stage is drawn with a heavy
border. Where \code{\link[=plan_dot]{plan_dot()}} emits static Graphviz
text, \code{plan_view()} is the exploratory sibling: watch the plan
change shape as a pipeline is composed.
}
\examples{
\dontrun{
lr <- lazy_source("cube.tif")
plan_view(focal(lr * 2, radius = 1L, fn = g_mean))
}
}
\seealso{
\code{\link[=plan_dot]{plan_dot()}} for DOT text, \code{\link[=draw]{draw()}} for pixels.
}
