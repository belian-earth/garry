% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy_cog.R
\name{dequantize_esd}
\alias{dequantize_esd}
\title{Dequantize Embedded Seamless Data (ESD) FSQ codes.}
\usage{
dequantize_esd(x, level, levels = c(8L, 8L, 8L, 5L, 5L, 5L))
}
\arguments{
\item{x}{Packed FSQ codes (traced array or plain numeric).}

\item{level}{Which level to decode (index into \code{levels}).}

\item{levels}{Integer vector of per-level cardinalities. Default
\code{c(8L, 8L, 8L, 5L, 5L, 5L)} (the ESD quantiser).}
}
\value{
The decoded level, same shape as \code{x}, in \verb{[-1, 1]}.
}
\description{
Decodes ONE level from packed Finite Scalar Quantisation indices: each
uint16 code factorises into \code{length(levels)} per-level integers via the
positional basis \code{B = cumprod(c(1, levels[-length(levels)]))}, and each
rescales to roughly \verb{[-1, 1]}:
}
\details{
\deqn{c_j = \lfloor x / B_j \rfloor \bmod L_j, \quad
      v_j = (c_j - \lfloor L_j/2 \rfloor) / \lfloor L_j/2 \rfloor}

Written in the \verb{g_*} vocabulary so it fuses onto the read as a garry map
(one \code{\link[=lazy_map]{lazy_map()}} per band and level) -- on the device, not a separate
decode pass. Truncation toward zero is the double-cast idiom
\code{g_cast(g_cast(z, "i32"), "f32")} (XLA convert semantics); all
arithmetic is exact in f32 because codes are integers below
\code{prod(levels)} (64000 for the ESD default, well under 2^24). NaN
(nodata, D8) is re-masked explicitly after the decode: casting NaN to
an integer is undefined, so propagation through the casts cannot be
relied on.

The default \code{levels} matches the ESD upstream quantiser (12 monthly
uint16 bands x 6 levels = 72 embedding channels). Any FSQ-packed
product decodes by passing its own \code{levels}.
}
\references{
Chen S., et al. ESD quantiser:
\url{https://github.com/shuangchencc/ESD/blob/main/esd_quantizer.py}

Mentzer F., Minnen D., Agustsson E., Tschannen M. (2024). Finite
scalar quantization: VQ-VAE made simple. ICLR.
}
