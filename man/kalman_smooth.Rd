% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_kalman.R
\name{kalman_smooth}
\alias{kalman_smooth}
\title{Smooth a stack with the LLT Kalman smoother (mean + sd pair).}
\usage{
kalman_smooth(
  x,
  sigma_lvl,
  sigma_slp,
  sigma_obs = 1,
  obs_var = NULL,
  outputs = c("mean", "sd"),
  dtype = "f32",
  ...
)
}
\arguments{
\item{x}{Observation stack (\code{LazyRaster} with a \code{t} axis), or a
\code{LazyDataset} (each band smoothed independently).}

\item{sigma_lvl, sigma_slp, sigma_obs}{Noise standard deviations (level
disturbance, slope disturbance, observation).}

\item{obs_var}{Optional relative observation-variance stack on the
same grid (\code{Var(v_t) = sigma_obs^2 * obs_var_t}).}

\item{outputs}{Which outputs to build (\code{"mean"}, \code{"sd"}).}

\item{dtype}{Output dtype (default f32).}

\item{...}{Passed to \code{\link[=kalman_llt]{kalman_llt()}}.}
}
\value{
A named list of lazy objects, one per requested output.
}
\description{
Convenience wrapper: one \code{\link[=scan_over]{scan_over()}} per requested output, sharing
one \code{\link[=kalman_llt]{kalman_llt()}} parameterisation.
}
