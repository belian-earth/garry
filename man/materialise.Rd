% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/materialise.R
\name{materialise}
\alias{materialise}
\title{Materialise a lazy object locally and stay lazy.}
\usage{
materialise(
  x,
  dir = NULL,
  name = "garry",
  nodata = NULL,
  overwrite = FALSE,
  distributed = garry_daemons_set()
)
}
\arguments{
\item{x}{A \code{LazyDataset} or \code{LazyRaster}.}

\item{dir}{Directory for the cubes (created if missing); default: a
unique session-temporary directory.}

\item{name}{File-name stem (default \code{"garry"}).}

\item{nodata}{Optional sentinel for the written files, as in
\code{\link[=write_tif]{write_tif()}}.}

\item{overwrite}{Replace existing files at the target paths?}

\item{distributed}{As in \code{\link[=collect]{collect()}}.}
}
\value{
A lazy object of the same class as \code{x}, reading the local
cubes.
}
\description{
The checkpoint verb (dbplyr's \code{compute()} for rasters): execute the
current graph, write the results to local raw-BSQ cubes (\code{.vrt} +
\code{.bin}, the format any GDAL tool reads and garry re-reads ~9x faster
than tiled GeoTIFF), and return the SAME KIND of lazy object rebuilt
over the local files. Everything downstream continues unchanged;
nothing upstream (network reads, warps, masking, model inference)
runs again.
}
\details{
A \code{LazyDataset} writes one multiband cube per time slice through a
single multi-sink plan (all slices' reads drain together), carrying
band names, slice dates, and the \code{mask_asset} into the rebuilt
dataset; ragged bands (a band missing some slices) survive. A
\code{LazyRaster} writes one cube and reopens it, which is also the
sanctioned route around the v1 "cannot warp a computed raster" rule:
\code{align(materialise(x, dir), grid)}.

Files land at \verb{dir/name-<slice>.vrt} (dataset) or \code{dir/name.vrt}
(raster). Existing files are refused unless \code{overwrite = TRUE}:
the graph may have changed since they were written, and silently
reusing stale pixels is the failure mode a checkpoint must not have.

\code{dir} defaults to a fresh unique directory under the session's
\code{\link[=tempdir]{tempdir()}}, announced by a message: convenient, but session-scoped
(the files vanish when R exits), and every call writes a NEW copy,
so repeated interactive re-runs accumulate until the session ends.
For large cubes, or to keep or reuse a checkpoint, give a real
directory (note some systems mount \verb{/tmp} in RAM).
}
