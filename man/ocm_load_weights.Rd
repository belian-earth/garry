% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocm_weights.R
\name{ocm_load_weights}
\alias{ocm_load_weights}
\title{Load and fold OmniCloudMask weights.}
\usage{
ocm_load_weights(dir, models = c("regnety", "edgenext"))
}
\arguments{
\item{dir}{Directory containing the OCM v4 \code{.safetensors} files.}

\item{models}{Which ensemble members to load
(\code{"regnety"}, \code{"edgenext"}).}
}
\value{
List with \code{weights} (per model), \code{kernel_id}, \code{paths}.
}
\description{
Reads the OCM v4 safetensors state dicts, folds batch norms into
their convolutions, and returns the nested weight lists the native
forward pass consumes, cached as an \code{.rds} under
\code{tools::R_user_dir("garry", "cache")} keyed by the content hash of
the weight files (the same hash is the model's jit-cache identity).
}
\details{
Weights are not distributed with garry: point \code{dir} at a directory
holding the official OmniCloudMask model files (for example the
Python package's download cache,
\verb{~/.local/share/omnicloudmask/<version>/}).
}
