% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_points.R
\name{extract_points}
\alias{extract_points}
\title{Extract raster values at points.}
\usage{
extract_points(raster, xy, bands = NULL, interp = NULL, ...)
}
\arguments{
\item{raster}{A \code{LazyRaster}, \code{LazyDataset}, raster path, or
\code{GDALRaster} object.}

\item{xy}{Points: a \code{\link[wk:xy]{wk::xy()}} vector (its CRS supplies \code{xy_srs}), or a
two-column matrix/data frame as gdalraster expects.}

\item{bands}{Bands to extract (default all).}

\item{interp}{Interpolation: \code{NULL}/\code{"nearest"} (default), \code{"bilinear"},
\code{"cubic"}, \code{"cubicspline"}.}

\item{...}{Passed to \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}} (\code{krnl_dim},
\code{xy_srs}, \code{max_ram}, \code{as_data_frame}).}
}
\value{
As \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}}: a matrix, or a data frame with
\code{as_data_frame = TRUE}.
}
\description{
Samples a raster at points via \code{\link[gdalraster:pixel_extract]{gdalraster::pixel_extract()}}, which reads
only the blocks containing points. Accepts anything that function accepts
(a path or a \code{GDALRaster}, passed through untouched) plus a garry
\code{LazyRaster}/\code{LazyDataset} that is already \strong{materialised} -- i.e. one
whose graph is a bare source over local files, as \code{\link[=materialise]{materialise()}}
returns.
}
\details{
An unmaterialised pipeline is an error, not a silent cube write. Getting
pixels onto disk costs real time and space, and the cube is almost always
wanted again (for the predict pass, say), so the caller should own that
step:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cube <- materialise(pipeline)     # explicit, reusable
vals <- extract_points(cube, pts)
}\if{html}{\out{</div>}}

\code{xy} may be a \code{\link[wk:xy]{wk::xy()}} point vector, in which case its CRS supplies
\code{xy_srs} and GDAL reprojects as needed; a matrix or data frame behaves
exactly as in gdalraster.
}
\examples{
\dontrun{
pts <- wk::xy(c(512300, 514800), c(4600100, 4601900), crs = "EPSG:32632")
cube <- materialise(composite)
extract_points(cube, pts, interp = "bilinear")
extract_points("composite.tif", pts)   # a path works too
}
}
\seealso{
\code{\link[=materialise]{materialise()}}, \code{\link[=collect]{collect()}}
}
