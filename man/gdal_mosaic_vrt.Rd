% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_adapter.R
\name{gdal_mosaic_vrt}
\alias{gdal_mosaic_vrt}
\title{Mosaic already-grid-aligned rasters into a VRT (adapter).}
\usage{
gdal_mosaic_vrt(dst, files, te = NULL, ts = NULL, vrtnodata = NULL)
}
\arguments{
\item{dst}{Output VRT path.}

\item{files}{Grid-aligned input rasters, low-to-high priority.}
}
\value{
\code{dst}.
}
\description{
\code{gdalbuildvrt} of same-grid single-band rasters: overlapping pixels take the
LAST input, so pass \code{files} in ascending priority (latest datetime last, to
match the highest-on-top overlap rule). Used to assemble multi-tile
mosaics (e.g. the file form of \code{\link[=lazy_dataset]{lazy_dataset()}}).
}
\details{
\code{gdalbuildvrt} takes only north-up sources in one projection and
SKIPS, with a warning, any other: a tile stored south-up (positive
north-south pixel size; every AEF tile, seen 2026-09-25) or a tile
in the neighbouring UTM zone (ESD tiles on the MGRS grid, which
overlaps a zone boundary). A skipped tile would be a hole, or with
every tile skipped no mosaic at all, so a build that lost a source is
an error here. Sources like that do not belong in a mosaic: the file
form of \code{\link[=lazy_dataset]{lazy_dataset()}} keeps them as a multi-path source node and
the warper reads them together (\code{\link[=gdal_warp_vrt]{gdal_warp_vrt()}}).
}
\keyword{internal}
