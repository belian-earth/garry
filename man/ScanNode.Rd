% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node.R
\name{ScanNode}
\alias{ScanNode}
\title{Scan along a named dim, preserving it. Barrier over \code{over}.}
\usage{
ScanNode(
  id = integer(0),
  parents = integer(0),
  grid = GridSpec(),
  over = character(0),
  direction = character(0),
  fn = list(),
  dtype = character(0)
)
}
\arguments{
\item{id}{Integer node id (assigned by \code{graph_add()}).}

\item{parents}{Integer ids of parent nodes.}

\item{grid}{Output \code{GridSpec} of this node.}

\item{over}{Single dim name to scan along (\code{"t"} or \code{"band"}).}

\item{direction}{One of \code{"forward"}, \code{"backward"}, \code{"bidir"}.}

\item{fn}{Length-1 list holding the scan body \code{fn(xs, margin)}.}

\item{dtype}{Length-0 (parent's dtype) or length-1 dtype override.}
}
\value{
A \code{ScanNode}.
}
\description{
The missing IR shape between \code{MapNode} and \code{ReduceNode}: carry state
sequentially along one non-spatial axis while emitting a same-length
series (Kalman smoothers, EWMA, IIR filters, cumulative ops). The
output grid is the parent grid unchanged (the scanned axis survives),
optionally with a dtype override.
}
\details{
The body kernel is \code{fn(xs, margin) -> y}: \code{xs} is the LIST of parent
chunk values (length >= 1; multi-parent scans read several cubes in
lockstep), \code{margin} is the scanned axis position from \code{.dim_margins},
and \code{y} has the same shape as \code{xs[[1]]}. Inside, the body uses
\code{g_scan()} to carry state along \code{margin}; everything else in the
chunk (the spatial axes) is batched through the carry. Like a custom
reducer, a scan cannot be decomposed across spatial chunks, so it is
supported over \code{t}/\code{band} only (each spatial chunk holds the full
axis).

\code{direction} is declarative metadata (drawn, hashed into the kernel
signature): \code{"bidir"} bodies run a forward and a reverse \code{g_scan()}
internally as ONE fused kernel, so forward state never crosses a node
boundary.
}
