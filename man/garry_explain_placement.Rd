% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placement.R
\name{garry_explain_placement}
\alias{garry_explain_placement}
\title{Explain the scheduler's placement decisions for a computation.}
\usage{
garry_explain_placement(
  x,
  read = NULL,
  compute = NULL,
  mode = garry_opt("placement")
)
}
\arguments{
\item{x}{A \code{LazyRaster}, a named list of them (multi-export), or a
\code{Plan}.}

\item{read, compute}{Pool widths to assume; default = the live pools
(0 when none are running).}

\item{mode}{\code{"rules"} or \code{"cost"}; default \code{garry_opt("placement")}.}
}
\value{
A data.frame with columns \code{source}, \code{compute}, \code{bands},
\code{flops_px}, \code{move_mb}, \code{cost_fuse_s}, \code{cost_mat_s}, \code{decision},
\code{reason}.
}
\description{
Runs the placement pass over
the plan the same way \code{collect(distributed = TRUE)} would, and
returns its decision table: one row per fusable source -> compute
chain with the decision, the modelled costs (cost mode), and the
reason. Placement depends on runtime resources, so pool widths are
read from the live \code{\link[=garry_daemons]{garry_daemons()}} pools when present; pass
\code{read} / \code{compute} to ask "what would the pass do with this
topology" without daemons running.
}
\seealso{
\code{\link[=garry_options]{garry_options()}}, \code{\link[=garry_task_report]{garry_task_report()}}
}
