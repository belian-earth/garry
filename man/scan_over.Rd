% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy_raster.R
\name{scan_over}
\alias{scan_over}
\title{Scan along an axis, keeping it (temporal recursions).}
\usage{
scan_over(x, fn, over = "t", direction = "forward", dtype = NULL, bands = NULL)
}
\arguments{
\item{x}{A \code{LazyRaster}, a list of \code{LazyRaster}s on the same graph
and grid (multi-input scan), or a \code{LazyDataset}.}

\item{fn}{Scan body \code{fn(xs, margin)}.}

\item{over}{Single dim name to scan along (default \code{"t"}).}

\item{direction}{\code{"forward"}, \code{"backward"}, or \code{"bidir"} (the body
encapsulates direction; this is declarative metadata).}

\item{dtype}{Optional output dtype override (default: input dtype).}

\item{bands}{\code{LazyDataset} only: bands to scan (default: all).}
}
\value{
A \code{LazyRaster} on the unchanged grid, or a \code{LazyDataset}.
}
\description{
The length-preserving sibling of \code{\link[=reduce_over]{reduce_over()}}: carry state
sequentially along \code{over} while emitting a same-length series
(Kalman smoothers, EWMA, IIR filters, cumulative custom ops). The
output grid is the input grid unchanged; only the dtype may differ.
}
\details{
\code{fn} is the scan body \code{fn(xs, margin) -> y}, written in the
\verb{g_*} vocabulary and typically built around \code{\link[=g_scan]{g_scan()}}: \code{xs} is the
LIST of parent chunk values (pass a list of LazyRasters on the same
grid to scan several cubes in lockstep), \code{margin} is the scanned
axis position, and \code{y} has the shape of \code{xs[[1]]}. Like a custom
reducer, a scan holds the full \code{over} axis per spatial chunk, so it
is supported over \code{"t"}/\code{"band"} only.

Over a \code{LazyDataset}, each band's slices are stacked along \code{"t"} and
scanned independently (\code{bands} restricts which).
}
