# A dataset partitioned into time groups (see [`group_by_time()`](https://belian-earth.github.io/garry/reference/group_by_time.md)).

A dataset partitioned into time groups (see
[`group_by_time()`](https://belian-earth.github.io/garry/reference/group_by_time.md)).

## Usage

``` r
LazyDatasetGroups(groups = list(), by = character(0))
```
