# Clamp values to a closed range, elementwise.

Clamp values to a closed range, elementwise.

## Usage

``` r
g_clamp(x, lo, hi)
```

## Arguments

- x:

  Traced array or plain numeric.

- lo, hi:

  Range bounds (scalars).

## Value

Same shape as `x`.
