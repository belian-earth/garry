# Can this node be composed with fusable neighbours into a single kernel?

Can this node be composed with fusable neighbours into a single kernel?

## Usage

``` r
fusable(node, ...)
```

## Arguments

- node:

  An IR `Node`.

- ...:

  Passed to methods.

## Value

`TRUE` or `FALSE`.
