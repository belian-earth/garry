# Execute a no-focal composite via the lean GDAL-direct cube path.

Execute a no-focal composite via the lean GDAL-direct cube path.

## Usage

``` r
.execute_composite_direct(
  plan,
  spec,
  path = NULL,
  nodata = NULL,
  band_names = NULL,
  wspec = NULL
)
```
