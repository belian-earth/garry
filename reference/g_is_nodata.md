# Is a value nodata (NaN under the D8 sentinel model)?

Is a value nodata (NaN under the D8 sentinel model)?

## Usage

``` r
g_is_nodata(x)
```

## Arguments

- x:

  Numeric array.

## Value

Logical array.
