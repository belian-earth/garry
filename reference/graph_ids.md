# All node ids in the graph, in insertion order.

All node ids in the graph, in insertion order.

## Usage

``` r
graph_ids(graph)
```

## Arguments

- graph:

  A `Graph`.

## Value

Sorted integer vector of node ids.
