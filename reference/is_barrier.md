# Does this node force a stage boundary?

Does this node force a stage boundary?

## Usage

``` r
is_barrier(node, ...)
```

## Arguments

- node:

  An IR `Node`.

- ...:

  Passed to methods.

## Value

`TRUE` or `FALSE`.
