# GDAL runtime version as a human string (adapter).

GDAL runtime version as a human string (adapter).

## Usage

``` r
gdal_version_str()
```

## Value

Character, e.g. `"GDAL 3.9.0, released ..."`.
