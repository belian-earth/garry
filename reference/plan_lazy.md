# Plan a LazyRaster: run all planner passes and export a Plan.

Plan a LazyRaster: run all planner passes and export a Plan.

## Usage

``` r
plan_lazy(x)
```

## Arguments

- x:

  A `LazyRaster`.

## Value

A `Plan`.
