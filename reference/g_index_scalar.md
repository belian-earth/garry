# Extract element `i` of a 1-D array as a scalar (static index).

Extract element `i` of a 1-D array as a scalar (static index).

## Usage

``` r
g_index_scalar(v, i)
```

## Arguments

- v:

  1-D array (e.g. a flattened kernel).

- i:

  1-based static index.

## Value

Scalar.
