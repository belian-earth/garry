# Is `dtype` a member of garry's (anvl-aligned) dtype vocabulary?

Is `dtype` a member of garry's (anvl-aligned) dtype vocabulary?

## Usage

``` r
dtype_valid(dtype)
```

## Arguments

- dtype:

  A dtype string, e.g. `"f32"`.

## Value

`TRUE` or `FALSE`.
