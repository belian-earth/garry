# Download an AnvlArray (or a nested list of them) to R arrays.

Download an AnvlArray (or a nested list of them) to R arrays.

## Usage

``` r
g_download(x)
```

## Arguments

- x:

  `AnvlArray` or (nested) list.

## Value

R array / nested list of R arrays.
