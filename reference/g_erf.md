# Error function (exact GELU building block).

Error function (exact GELU building block).

## Usage

``` r
g_erf(x)
```

## Arguments

- x:

  Numeric array (traced or plain).

## Value

`erf(x)` elementwise.
