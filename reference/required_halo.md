# Halo radius required by this node from its inputs.

Halo radius required by this node from its inputs.

## Usage

``` r
required_halo(node, ...)
```

## Arguments

- node:

  An IR `Node`.

- ...:

  Passed to methods.

## Value

Integer halo radius in pixels.
